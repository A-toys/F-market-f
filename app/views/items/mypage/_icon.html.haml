= link_to profile_user_path do
  .wrapping
    .myicon
      .myicon__img
        - if @user.icon?
          = image_tag "#{user.icon}", size: "50x50", id: "my_icon"
        - else
          = image_tag "/myicon.png", size: "50x50", id: "my_icon"
      .myicon__name
        = @user.nickname
      .myicon__number
        評価  0   出品数  0
